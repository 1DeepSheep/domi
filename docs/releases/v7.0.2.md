# domi 7.0.2

修复 PLAUD 转写已经提交、但读取文字稿时遇到短暂网络错误，就被标为失败的问题。同步会自动重试读取；等待超时后保留任务，后续继续补下载已有文字稿。

生成请求提交前保存状态，确认成功后保存回执。重复同步或重启后复用原录音；请求结果不确定时只检查与下载，不自动重复提交生成。

录音列表分别显示等待生成、等待同步和需要处理的状态。部分完成与失败使用对应提示，刷新后清除旧的失败数量，不再一律显示绿色“同步完成”或“需要重试”。

这些恢复由程序执行，不调用模型，不增加模型 token 消耗，也不减少纪要内容或跳过质量检查。本版本内置 domi 插件 7.0.3。
