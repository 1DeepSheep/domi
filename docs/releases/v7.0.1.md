# domi 7.0.1

任务在后台完成后，domi 会发送 macOS 系统通知，并在任务列表显示未读红点；Dock 图标显示未读任务数量。点击通知可直接打开对应任务，查看后清除未读提醒。

正在前台查看的任务完成时不重复打扰。失败与等待补充信息使用各自的提示，主动停止不会显示成功通知。提醒在任务结果及必要的归档结算处理后触发，同一次完成事件不会重复推送。

未读状态保存在本地，重新打开客户端后仍可查看。提醒完全由客户端触发，不额外调用模型，也不增加 token 消耗。

本版本内置 domi 插件 7.0.2，保留纪要原文覆盖、信息完整性和格式校验。
